import { Alert } from "react-bootstrap";

const LandingNotice = () => {
  return (
    <Alert variant="success" className="flex-center">
      Welcome, enter a last name above to begin your search
    </Alert>
  );
};
export default LandingNotice;
